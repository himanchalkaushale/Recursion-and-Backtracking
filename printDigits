public static void printDigits(int n){
        //base case
        if(n==0) return;
        //recurrence relation
        printDigits(n/10);
         //processing
        System.out.print(n%10+" ");
    }

Note:- this is type of head recursion where recurrence relation is given before the processing code and tail recursion is vice versa 
